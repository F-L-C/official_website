import '../app/globals.css'

export default function Contacts() {
    return (
        <>
         <section>
            <h2 className="sectionTitle">Contact</h2>
            {/* Add contact information (mail, discord, etc.) */}
        </section>
        </>
    )
}